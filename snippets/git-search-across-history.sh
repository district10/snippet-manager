# https://git-scm.com/docs/git-log, git grep across history
git log -p -S<KEYWORD>
#   -p (show diff), -p <file> (generate patch)
#   --all, --branches[=<pattern>], --tags[=<pattern>]
#   -S<TEXT>, -G<REGEX>
