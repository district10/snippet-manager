git config --global -l
git config --global -l diff.tool=vimdiff
git config --global credential.helper "cache --timeout=3600"
git config --global http.sslVerify false
git config --global core.quotepath off # utf-8 显示文件名, git filename utf-8, display characters incorrectly
